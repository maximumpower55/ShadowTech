package ml.visualax.shadowtech

import net.minecraft.item.Item

class ModItems {
    companion object {
        var STEEL_INGOT = Item(Item.Settings().group(ShadowTechMod.MATRIALS_ITEM_GROUP))
        var COPPER_INGOT = Item(Item.Settings().group(ShadowTechMod.MATRIALS_ITEM_GROUP))
        var TIN_INGOT = Item(Item.Settings().group(ShadowTechMod.MATRIALS_ITEM_GROUP))
    }
}

